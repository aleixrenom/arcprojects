You are reviewing Finnish-English translations for a vocabulary quiz. For each entry, check:

1. The "correct" value is an accurate English translation of the Finnish "word". If it's wrong, fix it (and update "options" so the new value is included).
2. No option other than "correct" is an acceptable translation of the Finnish word. Note that some Finnish words appear in multiple entries with different correct answers (e.g. "hän" → "he" and "she") — those other correct answers must not appear as distractors.
3. No two options are synonyms or near-synonyms of each other. The quiz doesn't need to be difficult: if there's any doubt, replace a distractor with a clearly unrelated word.

When editing, preserve these existing invariants: exactly 4 options per entry, "correct" appears first in "options", no duplicate words within an options array.

Only edit entries that violate a rule, with targeted edits that preserve the JSON formatting. Don't comment on entries that are fine.
